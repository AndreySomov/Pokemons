import React, { FC } from 'react';

import { Container, StyledImg } from './stylex';

const Loading: FC = () => (
  <Container>
    <StyledImg src="https://pokeres.bastionbot.org/pokeball.gif" alt="" />
  </Container>
);

export default Loading;
